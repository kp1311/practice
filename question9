//Function to remove duplicates form an array in place in O(n)  time and O(1) space.

int remove_duplicate(int A[],int N)
{
    int j=0;
    int i=0;
    
    for( i=0;i<N-1;i++)
    { if(A[i]!=A[i+1])
    
    {
        A[j]=A[i];
        j++;
    }
        
        
    }
    A[j]=A[N-1];
return (j+1);
    
}
